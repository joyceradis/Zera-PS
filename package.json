{
  "name": "zera-ps",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check assets/app.js && node --check assets/ui.js && node --check assets/data.js && node --check assets/templates.js && node --check assets/scores.js && node --check assets/clinical-state.js && node --check assets/document-engine.js && node --check assets/storage.js && node --check service-worker.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}

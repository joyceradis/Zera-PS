name: Completed branch prune (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-off branch prune completed; retained temporarily only because workflow file deletion is connector-restricted.'

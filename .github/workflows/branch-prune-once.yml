name: Completed branch prune (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Residual branch audit/prune completed; workflow retained disabled because connector deletion is restricted.'
